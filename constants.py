# Shape constants
SHAPE_CONE = "cone"
SHAPE_SPHERE = "sphere"

# Example usage
selected_shape = SHAPE_CONE

if selected_shape == SHAPE_CONE:
    print("Selected shape is a cone.")
elif selected_shape == SHAPE_SPHERE:
    print("Selected shape is a sphere.")
else:
    print("Unknown shape.")
